import React, { Component } from 'react';

class Header extends Component{
    render(){
        return <header className="App-header">
        <h1 className="App-title">Admin Dashboard for analytics</h1>
      </header>
    }
}

export default Header;